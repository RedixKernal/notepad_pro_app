<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.SVGPath?>

<BorderPane xmlns="http://javafx.com/javafx/21"
    xmlns:fx="http://javafx.com/fxml/1" fx:id="rootPane" fx:controller="com.ravi.notesapp.controller.MainController" styleClass="root-pane">

    <!-- ==================== HEADER ==================== -->
    <top>
        <VBox>
            <!-- ==================== TOOLBAR ==================== -->
            <ToolBar fx:id="toolBar" styleClass="tool-bar" style="-fx-padding: 3 8 3 8; -fx-min-height: 32px; -fx-pref-height: 32px;">
                <MenuButton fx:id="btnMoreOptions" text="File" styleClass="toolbar-btn, file-menu-btn">
                    <items>
                        <MenuItem text="Open File..." onAction="#onOpenFile"/>
                        <Menu fx:id="menuRecent" text="Open Recent"/>
                        <SeparatorMenuItem/>
                        <MenuItem text="Save All" onAction="#onSaveAll"/>
                        <CheckMenuItem fx:id="menuAutoSave" text="Auto Save" onAction="#onToggleAutoSave"/>
                        <MenuItem text="Close Saved" onAction="#onCloseSaved"/>
                        <SeparatorMenuItem/>
                        <MenuItem text="Find in Files..." onAction="#onFindInFiles"/>
                        <SeparatorMenuItem/>
                        <MenuItem text="Exit" onAction="#onExit"/>
                    </items>
                </MenuButton>

                <Button fx:id="btnSave" text="Save" onAction="#onSave" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="Save Active File (Ctrl+S)"/>
                    </tooltip>
                </Button>



                <Button fx:id="btnDelete" text="Delete" onAction="#onDelete" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="Move to Recycle Bin (Del)"/>
                    </tooltip>
                </Button>

                <Button fx:id="btnSearch" text="Find" onAction="#onFind" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="Find in File (Ctrl+F)"/>
                    </tooltip>
                </Button>

                <Button fx:id="btnTheme" text="Theme" onAction="#onToggleTheme" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="Toggle Theme (Ctrl+Shift+T)"/>
                    </tooltip>
                </Button>

                <Button fx:id="btnAbout" text="About" onAction="#onAbout" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="About Notepad_Pro"/>
                    </tooltip>
                </Button>
                <Region HBox.hgrow="ALWAYS"/>

                <Button fx:id="btnRun" text="" onAction="#onRunActiveFile" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="Run Active File (Ctrl+R)"/>
                    </tooltip>
                    <graphic>
                        <SVGPath content="M8 5v14l11-7z" styleClass="svg-icon" fill="green"/>
                    </graphic>
                </Button>

                <ToggleButton fx:id="btnToggleAi" text="AI" onAction="#onToggleAi" styleClass="toolbar-btn" graphicTextGap="4">
                    <tooltip>
                        <Tooltip text="Toggle AI Assistant (Ctrl+J)"/>
                    </tooltip>
                </ToggleButton>
            </ToolBar>
        </VBox>
    </top>

    <!-- ==================== MAIN SPLIT ==================== -->
    <center>
        <SplitPane fx:id="splitPane" dividerPositions="0.22" styleClass="main-split">


            <!-- RIGHT: Editor panel -->
            <fx:include fx:id="editorPane" source="editor.fxml"/>

            <!-- FAR RIGHT: AI Chat panel -->
            <fx:include fx:id="aiChatPane" source="ai_chat.fxml"/>

        </SplitPane>
    </center>

    <!-- ==================== STATUS BAR ==================== -->
    <bottom>
        <HBox fx:id="statusBar" styleClass="status-bar" alignment="CENTER_LEFT" spacing="10">
            <padding>
                <Insets top="4" right="12" bottom="4" left="12"/>
            </padding>
            <Label fx:id="lblStatus" text="Ready" styleClass="status-text"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="lblFileInfo" text="" styleClass="status-meta"/>
        </HBox>
    </bottom>


</BorderPane>
